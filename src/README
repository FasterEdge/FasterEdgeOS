# FasterEdgeOS 构建说明

## 一、构建入口

在 Linux 构建主机上进入本目录，执行：

```bash
./build_fasteredgeos.sh
```

也可以使用 Makefile：

```bash
make all
```

构建会依次完成 Linux 内核、glibc、BusyBox、overlay、rootfs 和 BIOS/UEFI 镜像。

## 二、构建依赖

Debian/Ubuntu 可安装：

```bash
sudo apt update
sudo apt install -y wget make gawk gcc bc bison flex xorriso \
  libelf-dev libssl-dev file cpio gzip xz-utils
```

需要 UEFI、镜像或 QEMU 测试时，再安装对应的 `dosfstools`、`qemu-system-*` 和挂载工具。

## 三、配置

主配置文件为 `.config`，常用选项：

```text
FIRMWARE_TYPE=bios       # bios / uefi / both
OVERLAY_TYPE=folder      # folder / sparse
OVERLAY_LOCATION=iso     # iso / rootfs
BUILD_KERNEL_MODULES=false
JOB_FACTOR=1
```

`OVERLAY_BUNDLES` 用于选择要自动打入系统的组件。FasterEdgeOS 自有组件应使用独立 bundle 接入，例如：

```text
OVERLAY_BUNDLES=dhcp,fasteredgeos
```

## 四、构建产物

构建完成后，产物位于当前 `src` 目录：

```text
fasteredgeos.iso
fasteredgeos_image.tgz
```

生成的系统名称、`/etc/os-release`、默认主机名和启动菜单应统一使用 FasterEdgeOS。

## 五、overlay bundle 开发

新增软件放在：

```text
minimal_overlay/bundles/<bundle-name>/
```

bundle 通常包含：

```text
01_get.sh       获取源码或准备输入
02_build.sh     编译
03_install.sh   安装到 overlay
bundle.sh       bundle 入口或辅助脚本
```

FasterEdgeOS 服务建议安装到：

```text
/usr/bin/
/etc/fasteredgeos/
/etc/init.d/
/var/lib/fasteredgeos/
/var/log/fasteredgeos/
```

## 六、启动与测试

```bash
./qemu-bios.sh
./qemu-uefi.sh
```

写入 USB 前必须确认设备路径，避免误清除数据：

```bash
sudo ./write_to_media.sh /dev/sdX
```

## 七、清理

```bash
make clean
```

清理会删除构建工作目录和生成的镜像，但保留配置及显式指定的本地源码。

## 八、注意事项

- 构建脚本使用 POSIX Shell，任一步骤失败都会停止构建。
- UEFI 和部分镜像步骤需要 root 权限。
- 远程更新和系统管理服务必须校验签名，并保留可回滚版本。
- FasterEdge2Api 的生产部署必须使用 HTTPS 或可信 TLS 反向代理。
- 本目录只负责基础系统构建；FasterEdge 进程管理、系统管理和远程更新通过专用 overlay bundle 集成。
