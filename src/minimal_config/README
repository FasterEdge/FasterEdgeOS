构建流程允许你为内核和 Busybox 使用预定义配置文件。下面是使用你自己的
预定义配置文件的步骤：

1) 首先你需要一个 '.config' 配置文件。这里假设你已经了解 '.config' 文件是什么，
   以及它在内核与 Busybox 构建过程中的用途。把 '.config' 放入本目录并重命名。
   预定义配置文件名称为：

   kernel.config  - 内核配置文件
   busybox.config - Busybox 配置文件

2) 在主 '.config' 文件中把对应属性改为 'true'：

   USE_PREDEFINED_KERNEL_CONFIG  - 内核
   USE_PREDEFINED_BUSYBOX_CONFIG - Busybox

3) 完成。之后构建过程将不再生成默认 '.config' 文件，而是使用预定义配置文件。
   这在你想试验大量非默认配置选项时特别有用——直接使用配置文件比通过脚本生成
   大量选项更简单。另一个常见场景是：当你基于 FasterEdgeOS 分发自己的系统，
   并希望默认就使用非默认的内核和/或 Busybox 选项时。